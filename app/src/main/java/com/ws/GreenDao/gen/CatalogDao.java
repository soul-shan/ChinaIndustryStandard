package com.ws.GreenDao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ws.GreenDao.Catalog;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CATALOG".
*/
public class CatalogDao extends AbstractDao<Catalog, Void> {

    public static final String TABLENAME = "CATALOG";

    /**
     * Properties of entity Catalog.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", false, "ID");
        public final static Property Specialitiesid = new Property(1, Integer.class, "specialitiesid", false, "SPECIALITIESID");
        public final static Property Chapterid = new Property(2, Integer.class, "chapterid", false, "CHAPTERID");
        public final static Property Chapter = new Property(3, String.class, "chapter", false, "CHAPTER");
    }


    public CatalogDao(DaoConfig config) {
        super(config);
    }
    
    public CatalogDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CATALOG\" (" + //
                "\"ID\" INTEGER," + // 0: id
                "\"SPECIALITIESID\" INTEGER," + // 1: specialitiesid
                "\"CHAPTERID\" INTEGER," + // 2: chapterid
                "\"CHAPTER\" TEXT);"); // 3: chapter
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CATALOG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Catalog entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer specialitiesid = entity.getSpecialitiesid();
        if (specialitiesid != null) {
            stmt.bindLong(2, specialitiesid);
        }
 
        Integer chapterid = entity.getChapterid();
        if (chapterid != null) {
            stmt.bindLong(3, chapterid);
        }
 
        String chapter = entity.getChapter();
        if (chapter != null) {
            stmt.bindString(4, chapter);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Catalog entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer specialitiesid = entity.getSpecialitiesid();
        if (specialitiesid != null) {
            stmt.bindLong(2, specialitiesid);
        }
 
        Integer chapterid = entity.getChapterid();
        if (chapterid != null) {
            stmt.bindLong(3, chapterid);
        }
 
        String chapter = entity.getChapter();
        if (chapter != null) {
            stmt.bindString(4, chapter);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Catalog readEntity(Cursor cursor, int offset) {
        Catalog entity = new Catalog( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // specialitiesid
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // chapterid
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // chapter
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Catalog entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setSpecialitiesid(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setChapterid(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setChapter(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Catalog entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Catalog entity) {
        return null;
    }

    @Override
    public boolean hasKey(Catalog entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
