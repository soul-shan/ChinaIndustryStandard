package com.ws.GreenDao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ws.GreenDao.Part;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PART".
*/
public class PartDao extends AbstractDao<Part, Void> {

    public static final String TABLENAME = "PART";

    /**
     * Properties of entity Part.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", false, "ID");
        public final static Property Catalogid = new Property(1, Integer.class, "catalogid", false, "CATALOGID");
        public final static Property Partid = new Property(2, Integer.class, "partid", false, "PARTID");
        public final static Property Part = new Property(3, String.class, "part", false, "PART");
    }


    public PartDao(DaoConfig config) {
        super(config);
    }
    
    public PartDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PART\" (" + //
                "\"ID\" INTEGER," + // 0: id
                "\"CATALOGID\" INTEGER," + // 1: catalogid
                "\"PARTID\" INTEGER," + // 2: partid
                "\"PART\" TEXT);"); // 3: part
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PART\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Part entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer catalogid = entity.getCatalogid();
        if (catalogid != null) {
            stmt.bindLong(2, catalogid);
        }
 
        Integer partid = entity.getPartid();
        if (partid != null) {
            stmt.bindLong(3, partid);
        }
 
        String part = entity.getPart();
        if (part != null) {
            stmt.bindString(4, part);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Part entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer catalogid = entity.getCatalogid();
        if (catalogid != null) {
            stmt.bindLong(2, catalogid);
        }
 
        Integer partid = entity.getPartid();
        if (partid != null) {
            stmt.bindLong(3, partid);
        }
 
        String part = entity.getPart();
        if (part != null) {
            stmt.bindString(4, part);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Part readEntity(Cursor cursor, int offset) {
        Part entity = new Part( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // catalogid
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // partid
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // part
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Part entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setCatalogid(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPartid(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setPart(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Part entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Part entity) {
        return null;
    }

    @Override
    public boolean hasKey(Part entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
