package com.ws.GreenDao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.ws.GreenDao.Paragraph;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PARAGRAPH".
*/
public class ParagraphDao extends AbstractDao<Paragraph, Void> {

    public static final String TABLENAME = "PARAGRAPH";

    /**
     * Properties of entity Paragraph.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", false, "ID");
        public final static Property Specialitiesid = new Property(1, Integer.class, "specialitiesid", false, "SPECIALITIESID");
        public final static Property Chapterid = new Property(2, Integer.class, "chapterid", false, "CHAPTERID");
        public final static Property Partid = new Property(3, Integer.class, "partid", false, "PARTID");
        public final static Property Paragraphid = new Property(4, Integer.class, "paragraphid", false, "PARAGRAPHID");
        public final static Property Paragraph = new Property(5, String.class, "paragraph", false, "PARAGRAPH");
        public final static Property Item = new Property(6, Integer.class, "item", false, "ITEM");
    }


    public ParagraphDao(DaoConfig config) {
        super(config);
    }
    
    public ParagraphDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PARAGRAPH\" (" + //
                "\"ID\" INTEGER," + // 0: id
                "\"SPECIALITIESID\" INTEGER," + // 1: specialitiesid
                "\"CHAPTERID\" INTEGER," + // 2: chapterid
                "\"PARTID\" INTEGER," + // 3: partid
                "\"PARAGRAPHID\" INTEGER," + // 4: paragraphid
                "\"PARAGRAPH\" TEXT," + // 5: paragraph
                "\"ITEM\" INTEGER);"); // 6: item
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PARAGRAPH\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Paragraph entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer specialitiesid = entity.getSpecialitiesid();
        if (specialitiesid != null) {
            stmt.bindLong(2, specialitiesid);
        }
 
        Integer chapterid = entity.getChapterid();
        if (chapterid != null) {
            stmt.bindLong(3, chapterid);
        }
 
        Integer partid = entity.getPartid();
        if (partid != null) {
            stmt.bindLong(4, partid);
        }
 
        Integer paragraphid = entity.getParagraphid();
        if (paragraphid != null) {
            stmt.bindLong(5, paragraphid);
        }
 
        String paragraph = entity.getParagraph();
        if (paragraph != null) {
            stmt.bindString(6, paragraph);
        }
 
        Integer item = entity.getItem();
        if (item != null) {
            stmt.bindLong(7, item);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Paragraph entity) {
        stmt.clearBindings();
 
        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer specialitiesid = entity.getSpecialitiesid();
        if (specialitiesid != null) {
            stmt.bindLong(2, specialitiesid);
        }
 
        Integer chapterid = entity.getChapterid();
        if (chapterid != null) {
            stmt.bindLong(3, chapterid);
        }
 
        Integer partid = entity.getPartid();
        if (partid != null) {
            stmt.bindLong(4, partid);
        }
 
        Integer paragraphid = entity.getParagraphid();
        if (paragraphid != null) {
            stmt.bindLong(5, paragraphid);
        }
 
        String paragraph = entity.getParagraph();
        if (paragraph != null) {
            stmt.bindString(6, paragraph);
        }
 
        Integer item = entity.getItem();
        if (item != null) {
            stmt.bindLong(7, item);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Paragraph readEntity(Cursor cursor, int offset) {
        Paragraph entity = new Paragraph( //
            cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // specialitiesid
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // chapterid
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // partid
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // paragraphid
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // paragraph
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6) // item
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Paragraph entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setSpecialitiesid(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setChapterid(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setPartid(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setParagraphid(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setParagraph(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setItem(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Paragraph entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Paragraph entity) {
        return null;
    }

    @Override
    public boolean hasKey(Paragraph entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
